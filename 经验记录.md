# 四轮成本计算 - 踩坑日志

## 1. 长途省份下拉无法点击选中

现象：输入省份名（如"福建"），下拉显示价格但点不进去。
根因：
- 事件委托 closest(".search-results .item") 永远返回 null
- 因为 .search-results .item 是后代选择器，closest()要求同一元素同时匹配
- search-results 和 item 分布在父子两个元素上，无单一元素同时拥有
解决：closest(".item") + item.closest("#searchResults") 分开检查

## 2. 价格属性名不统一

长途 item 用 data-pr，事件委托读 data-price，price 为 null
解决：item.getAttribute("data-price") || item.getAttribute("data-pr") 兜底

## 3. 中文全角括号解析失败

正则 /[?(][^?)]*[?)]/ 只匹配半角 ()，数据用全角（）
解决：正则改为 /[（(][^（)]*[）)]/ 同时匹配

## 4. pickProvince 死代码

引用未定义变量 L 和不存在的 DOM 元素 cityNote。直接删除。

## 5. GitHub Push 因 Token 被拦截

HANDOFF.md 含 Vercel/GitHub Token，push 被 Secret Scanning 拦截
解决：用占位符替换后重新 amend 提交

## 6. PowerShell 环境编码陷阱

python -c 中花括号/分号/双引号/尖括号都会被 PowerShell 作为语法处理
中文路径在 | python 管道传递时丢失编码
解决：复杂脚本写 .py 文件后用 python xxx.py 执行
